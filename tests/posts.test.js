// const PostController = require("../controllers/posts.controller");

// postController = new PostController();

// describe("this.postController.getPosts", () => {
//   const res = {
//     status: jest.fn(() => res),
//     send: jest.fn(),
//   };
//   const  = jest.fn();

// //   test("로그인 되어있지 않으면 nickname이 undefined", () => {
// //     expect(this.postController.getPosts.nickname).toEqual(undefined);,
// //     });

// });
